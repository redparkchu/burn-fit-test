import { StyleSheet } from "react-native";

export const CommonStyles = StyleSheet.create({
    app: {
        flex: 1
    },
    screen: {
        flex: 1,
        position: 'relative',
        backgroundColor: '#fff'
    },
    center: {
        flex:1,
        justifyContent: 'center',
        alignItems: 'center',
    }
});
